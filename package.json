{
  "name": "fullstack",
  "version": "1.0.0",
  "description": "A fullstack example of a pnpm workspace.",
  "main": "index.js",
  "scripts": {
    "start": "pnpm --stream -r start",
    "start:dev": "pnpm --stream -r run start:dev",
    "build": "pnpm --stream -r run build",
    "clean": "rm -rf .parcel-cache && pnpm -r run clean",
    "lint": "pnpm -r lint"
  },
  "keywords": [],
  "author": "amaprime07@gmail.com",
  "devDependencies": {
    "@nestjs/cli": "^10.2.1"
  },
  "engines": {
    "node": "^v20.9.0",
    "pnpm": "^8.9.2"
  }
}
